<%= turbo_frame_tag 'group_list' do%>
  <div>
    <h1 class="font-bold text-4xl">Groups</h1>
    <%= render GroupTabPanelComponent.new queries: @queries_details, current_query: @query  %>
    <%= render GroupComponent.with_collection(@groups, current_user: current_user) %>
  </div>
<% end %>
